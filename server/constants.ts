/** Default scroll/ticker speeds (in seconds). Higher = slower. */
export const DEFAULT_SPEEDS = {
  SERVICES: 8,
  EVENTS: 30,
  TICKER: 25,
} as const;

/** Default font settings for the services table. */
export const DEFAULT_FONTS = {
  SERVICES_FONT_SIZE: 18,
  NOTES_FONT_SIZE: 18,
  NOTES_FONT_WEIGHT: 700,
} as const;
